version: "2"
services:
    web:
        container_name: web-container
        build:
            context: ./
            dockerfile: web-dockerfile.txt
        ports:
            - "80:80"
            - "443:443"
        volumes:
            - ./webserver/:/webserver/
        networks:
            - default